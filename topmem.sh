#!/bin/bash

COLUMNS=999 ps aux --sort=-%mem | awk 'NR<=10{print $0}'
# ----------------------------------------------------------------------------
# ps -Af --sort=-%mem | awk 'NR<=10{print $0}'
# ----------------------------------------------------------------------------
exit 0
